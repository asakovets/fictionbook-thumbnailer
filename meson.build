project(
        'fictionbook-thumbnailer', 'c',
        version : '1.0',
        default_options : ['warning_level=2'])

deps = [
        dependency('gdk-pixbuf-2.0'),
        dependency('gio-2.0'),
        dependency('libarchive'),
        dependency('libxml-2.0')]

subdir('src')
